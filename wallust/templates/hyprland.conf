# Hyprland colors generated by wallust
# Template oficial adaptado de wallust-templates

general {
    # Bordes activos con degradado de 6 colores vibrantes
    # Usa saturate(0.6) para hacer los colores más llamativos
    col.active_border = rgb({{color1 | saturate(0.6) | strip}}) rgb({{color2 | saturate(0.6) | strip}}) rgb({{color3 | saturate(0.6) | strip}}) rgb({{color4 | saturate(0.6) | strip}}) rgb({{color5 | saturate(0.6) | strip}}) rgb({{color6 | saturate(0.6) | strip}})

    # Borde inactivo casi transparente (color0 + ee = muy transparente)
    col.inactive_border = rgba({{color0 | strip}}ee)
}